apiVersion: v1
kind: Service
metadata:
  name: {{ include "py7app.fullname" . }}
  labels:
    {{- include "py7app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "py7app.selectorLabels" . | nindent 4 }}
